apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.student_stats.StudentStatsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
