apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.feedback_service.FeedbackServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
