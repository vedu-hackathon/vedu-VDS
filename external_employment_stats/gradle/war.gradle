apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.external_employment_stats.ExternalEmploymentStatsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
