apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.online_course_stats.OnlineCourseStatsApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
