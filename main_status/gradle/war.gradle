apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.main_status.MainStatusApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
