apply plugin: "war"

bootWar {
    mainClassName = "ru.cepu.external_estimation.ExternalEstimationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
